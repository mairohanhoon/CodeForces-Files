#include<bits/stdc++.h>
using namespace std;
int main(){

    int t;
    cin >> t;
    while(t--){
        int x , y;
        cin >> x >> y ;
        string s;
        cin >> s;
        sort(s.begin(),s.end());
        cout << s << endl;
    }

    return 0;
}