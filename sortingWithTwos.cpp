#include<bits/stdc++.h>
using namespace std;

int main(){

    int t;
    cin >> t;
    while(t--){
        int n;
        cin >> n;
        int arr[n];
        for(int i=0; i<n; i++){
            cin >> arr[i];
        }
        for(int i=0; i<=n; i++){
            for(int j=0; j<pow(2,i); i++){
                if(j < n){
                    if()
                }
            }
        }
    }

    return 0;
}